apiVersion: v1
kind: Pod
metadata:
  name: secret-volume-demo

spec:
  containers:
    - name: nginx
      image: nginx:alpine

      volumeMounts:
        - name: secret-volume
          mountPath: /etc/secrets

  volumes:
    - name: secret-volume
      secret:
        secretName: app-secret
