apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: webapp2
  name: webapp2
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp2
  strategy: {}
  template:
    metadata:
      labels:
        app: webapp2
    spec:
      containers:
      - image: nginx
        name: nginx
        resources: {}
status: {}
